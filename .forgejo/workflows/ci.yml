
name: CI

on:
  pull_request:
    branches: [master]

jobs:
  provar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - run: npm ci

      - name: Tipos e build
        run: npm run build
