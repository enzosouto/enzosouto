# Currículo — Enzo Souto

Site pessoal de currículo, em Vue 3 + Vite + TypeScript. Bilíngue (PT/EN).

## Rodar localmente

```bash
npm install
npm run dev
```

## Build

```bash
npm run build   # gera dist/
npm run preview # serve o dist/ localmente
```
